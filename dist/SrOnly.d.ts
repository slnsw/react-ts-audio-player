import React from 'react';
import { IAudioPlayerConfig } from './Types';
interface IProps {
    children?: React.ReactNode;
    config?: IAudioPlayerConfig;
}
declare const SrOnly: React.FunctionComponent<IProps>;
export default SrOnly;
